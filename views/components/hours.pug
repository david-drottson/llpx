div.hours
  h3 Hours
  each day, dayName in hours
    if day
      p 
        span.day= dayName.charAt(0).toUpperCase() + dayName.slice(1)
        span.time #{day.open} - #{day.close}
    else
      p 
        span.day= dayName.charAt(0).toUpperCase() + dayName.slice(1)
        span.closed Closed

